[x] Create Local project using `git init`
[x] Create a github project
[x] Configure the local project to point to remote project using `git remote add` command
[x] Stage a file into git using `git add <filename>`
[x] Commit the staged changes using `git commit -m "<commit message>`
[x] Push the local changes to remote using `git push ---set-upstream origin <branch-name>`
[x] Pull the latest changes from git using `git pull`
[] Handle merge conflict
[] Delete a file and push it to git
[] Rename a file and push it to git
[] Create a branch in github directly and checkout in local
[] Create a branch in local and push to github
[] Merge the sub-branch into a main branch
[] Handle merge conflict when merging
[] Delete a local branch
[] Delete a remote branch
[] Rebase a local branch against a remote branch where we have new commits
[] Different merge strategy - Optional, will not be using it regularly
